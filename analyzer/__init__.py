from .analyzer import analyze_data

# This code defines the analyze_data function as part of the analyzer package.
# The __all__ variable is used to specify which functions are included in the package namespace
# when the package is imported using the from analyzer import * syntax.
# In this case, we want to include only the analyze_data function.


__all__ = ['analyze_data']
